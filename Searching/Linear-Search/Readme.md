## Linear Serach algorithm

## Binary-Search


        Begin
        for i := 0 to size -1 do
            if array[i] = key then
                return i
        done
        return invalid location
        End
